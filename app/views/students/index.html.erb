<% @students.each do |student| %>
  <section id=<%="student-#{student.id}"%>>
    <p><%= student.name %></p>
    <%= button_to "Delete", student_path(student.id), method: :delete %>
  </section>
<% end %>
